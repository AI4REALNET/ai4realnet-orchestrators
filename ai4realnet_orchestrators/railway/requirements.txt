# additional requirements for railway test runner
# pin pandas to < 3 because of new dedicated string data type by default in https://pandas.pydata.org/pandas-docs/stable/whatsnew/v3.0.0.html
pandas<3
# TODO use versioned dependency instead of branch
git+https://github.com/flatland-association/flatland-rl.git@main

# additional requirements for the robustness/resilience KPIs (069-077)
# see README_robustness_resilience_kpi_069_077.md
numpy
scipy
matplotlib
# torch is optional: without it the PPO, SAC and RLPerturb attackers fall back to
# a no-op and their KPI contributions are meaningless, so install it for real runs
torch
